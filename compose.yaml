name: codex-day

services:
  dashboard:
    build:
      context: .
    image: codex-day:0.5.0
    restart: unless-stopped
    ports:
      - "127.0.0.1:${CODEX_DAY_PORT:-8765}:8765"
    environment:
      TZ: "${TZ:-Asia/Shanghai}"
    volumes:
      - type: bind
        source: "${CODEX_DATA_DIR:?Set CODEX_DATA_DIR to your local .codex directory}"
        target: /codex
        read_only: true
      - type: volume
        source: index
        target: /data

volumes:
  index:
